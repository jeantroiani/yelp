<%= form_for @restaurant do |f| %>
	<%= f.label 	 :name 	 			%>
	<%= f.text_field :name	 			%>
	<%= f.label      :cuisine			%>
	<%= f.text_field :cuisine			%>
	<%= f.label      :phone_number		%>
	<%= f.text_field :phone_number		%>
	<%= f.submit 		  			 	%>
<% end %>
